version: "3.9"
services:
  frontend:
    build:
      context: ./FE
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./BE
    ports:
      - "5000:5000"
    